{
  "name": "looks-rare",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ gundamdweeb/looks-rare",
    "create-local": "graph create --node http://localhost:8020/ gundamdweeb/looks-rare",
    "remove-local": "graph remove --node http://localhost:8020/ gundamdweeb/looks-rare",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gundamdweeb/looks-rare"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
